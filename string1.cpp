#include<iostream>
#include<string>
using namespace std;
int main() 
    // Read a large number as a string
{
    string number;
    cin >> number; // Read the large number as a string
    int c= number.length();
    cout<<c<<endl;

    return 0;
}